if has('autocmd')
  filetype plugin indent on
endif
if has('syntax') && !exists('g:syntax_on')
  syntax enable
endif
:set expandtab
:set smarttab
:set tabstop=2
:retab
:set shiftwidth=2
:set nu
:set ruler
:set incsearch
:set paste
execute pathogen#infect()
